# Production environment -- loaded automatically by Vite during `vite build`.
# This is NOT a secret file. It contains only the public backend URL.
# Do NOT put JWT_SECRET, MONGODB_URI, passwords, or any sensitive values here.
VITE_API_BASE_URL=https://hostelcare-9od3.onrender.com/api/v1
